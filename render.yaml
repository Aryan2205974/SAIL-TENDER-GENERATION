services:
  - type: web
    name: tender-generation-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: api
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
      - key: LOW_RAM_MODE
        value: "true"
      - key: OPENROUTER_API_KEY
        value: "" # Configured via Render Dashboard

